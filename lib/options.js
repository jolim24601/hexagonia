var Hexscape = Hexscape || {};

Hexscape.Options = function () {};

Hexscape.Options.prototype = {
  preload: function (opts) {
    // background modal
  },
  create: function () {
    // mute
    // level select
    // show attempts
  }
}
